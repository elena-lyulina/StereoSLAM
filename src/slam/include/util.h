#pragma once

enum pointType
{
    BLOB_MAX,
    BLOB_MIN,
    CORNER_MAX,
    CORNER_MIN,

    TYPE_AMOUNT

};

enum frameNumber
{
    LEFT_PRED,
    RIGHT_PRED,
    RIGHT_SUCC,
    LEFT_SUCC,
    FRAME_AMOUNT

};

